# ============================================
# KRIRK Backend - Fase 1 MVP
# Python 3.11+ recomendado (3.14 pode ter
# incompatibilidades em pacotes de audio)
# ============================================

# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0.0
aiofiles>=24.0.0
psutil>=5.9.0

# LLM local
ollama>=0.4.0

# TTS (requer internet para edge-tts)
edge-tts>=6.1.0

# Audio (gravação de microfone)
sounddevice>=0.4.6
numpy>=1.26.0
scipy>=1.12.0

# STT - faster-whisper (Whisper local, sem internet)
faster-whisper>=1.0.0
# openai-whisper>=20240930  # alternativa, maior e mais lenta

# Memória vetorial (Fase 5)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Automação desktop (Fase 4)
# pyautogui>=0.9.54
# playwright>=1.45.0

# Web Search (Fase 4 — tools)
ddgs>=0.1.0

# Volume / Audio — usa PowerShell + Windows COM (sem dependências Python extras)

# Visão computacional (Fase 3)
mss>=9.0.0
pillow>=10.0.0
# easyocr>=1.7.0  # OCR separado — desnecessário com modelo de visão
